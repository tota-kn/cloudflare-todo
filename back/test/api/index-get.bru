meta {
  name: index-get
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/?text=hello
  body: none
  auth: none
}

tests {
  test("Response status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response contains stage and text", function() {
    expect(res.getBody()).to.have.property('message');
    expect(res.getBody().message).to.include('is OK; text=hello');
  });
}