meta {
  name: files-upload
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/v1/files
  body: text
  auth: none
}

body:text {
  Test file content for upload
}

tests {
  test("Response status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response contains success message", function() {
    expect(res.getBody()).to.have.property('message');
    expect(res.getBody().message).to.include('uploaded successfully');
  });
}