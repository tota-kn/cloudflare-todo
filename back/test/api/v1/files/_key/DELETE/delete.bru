meta {
  name: files-delete
  type: http
  seq: 1
}

delete {
  url: {{BASE_URL}}/v1/files/test-file.txt
  body: none
  auth: none
}

tests {
  test("Response status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response contains success message", function() {
    expect(res.getBody()).to.have.property('message');
    expect(res.getBody().message).to.include('deleted successfully');
  });
}