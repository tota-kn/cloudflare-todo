meta {
  name: todos-update-validation
  type: http
  seq: 3
}

put {
  url: {{BASE_URL}}/v1/todos/test-put-success-001
  body: json
  auth: none
}

body:json {
  {
    "title": "",
    "description": "This update should fail validation",
    "completed": true
  }
}

tests {
  test("Response status is 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
  
  test("Response contains validation error", function() {
    const body = res.getBody();
    expect(body).to.have.property('error');
  });
}