meta {
  name: v1-get
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/v1?title=Test&body=Body
  body: none
  auth: none
}

params:query {
  title: Test
  body: Body
}

tests {
  test("Response status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response contains success message", function() {
    expect(res.getBody().message).to.include('Hello from Cloudflare Workers!');
  });
}
