{
  "name": "cloudflare-todo",
  "version": "1.0.0",
  "workspaces": [
    "front",
    "back"
  ],
  "scripts": {
    "back": "pnpm --filter \"back\" run",
    "front": "pnpm --filter \"front\" run",
    "lint": "pnpm --filter \"back\" run lint && pnpm --filter \"front\" run lint"
  }
}